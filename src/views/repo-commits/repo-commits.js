import React from "react";

import Typography from "@material-ui/core/Typography";

const RepoCommits = () => {
  return <Typography variant="h3">Repo Commits</Typography>;
};

export default RepoCommits;
